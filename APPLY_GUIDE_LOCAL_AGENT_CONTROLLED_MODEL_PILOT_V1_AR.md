# دليل تطبيق Candidate — التجربة النموذجية المحلية المحكومة V1

## ماذا سيحدث عند التطبيق؟
سيضيف هذا الـCandidate فقط طبقة جاهزية لتجربة نموذج محلي مستقبلاً. ستبقى التجربة **معطلة افتراضياً** عبر الملف:
`config/local_agent_model_pilot_v1.json` وفيه `"enabled": false`.

## ما الذي لن يحدث؟
لن يتم تشغيل Ollama أو أي نموذج، ولن يتم فتح Shell أو Git أو نشر أو شبكة خارجية، ولن يتم تعديل `app.py` أو Workspace Core أو Command Center.

## التعديل البرمجي
يستبدل أربعة ملفات داخل `local_agent_core` ويضيف `model_pilot.py` وملف الإعداد، ويحدّث اختبار الوحدة الخاص بالـAgent Core. يُنشئ Installer نسخة احتياطية لكل ملف قبل تغييره.

## مراحل التطبيق
1. **Syntax**: فحص بنية الحزمة وPython وPowerShell.
2. **Preflight**: التأكد من أن ملفات المشروع ما تزال في الحالة المقبولة بعد V1 وأن إعداد التجربة لم يوجد مسبقاً.
3. **WhatIf**: محاكاة دون كتابة.
4. **Apply**: كتابة الملفات المعتمدة فقط بعد تفويض منفصل.
5. **Technical UAT**: اختبار أن التجربة معطلة وأن مسار المسودة يرفض الطلب دون تشغيل نموذج.

## المصطلحات
- **Candidate:** حزمة تغيير مقترحة لم تُطبق بعد.
- **Preflight:** فحص حالة المشروع قبل أي تعديل.
- **WhatIf:** محاكاة لنتيجة التطبيق بلا كتابة على المشروع.
- **Apply:** تنفيذ فعلي بعد تفويض صريح.
- **Model Pilot:** تجربة تشغيل نموذج محلي محدودة جداً؛ في هذه الحزمة هي جاهزة لكن معطلة.
