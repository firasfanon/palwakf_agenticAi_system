# دورة المهمة المحلية

```text
inbox/ → approved/ أو rejected/ → output/ + evidence/ + audit/
```

لا يؤدي وضع ملف داخل `approved/` إلى تنفيذ نموذج تلقائيًا في V1. التنفيذ معطّل افتراضيًا ويحتاج تشغيلًا مقيدًا في مرحلة لاحقة.
