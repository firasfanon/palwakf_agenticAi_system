(()=>{
"use strict";
const API="/api/v1/governed-operations";
const q=(selector,root=document)=>root.querySelector(selector);
const esc=value=>String(value??"").replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[char]));
const text=(value,fallback="—")=>String(value??"").trim()||fallback;
const state={route:location.hash.slice(1)||"dashboard",workspaceId:new URLSearchParams(location.search).get("workspace_id")||"palwakf_government",workspaces:[]};
async function get(path){const response=await fetch(API+path,{headers:{Accept:"application/json"}});const body=await response.json().catch(()=>({}));if(!response.ok)throw Error(body.detail?.code||`HTTP_${response.status}`);return body}
function path(suffix=""){return `/workspaces/${encodeURIComponent(state.workspaceId)}${suffix}`}
function pill(value,kind=""){return `<span class="pill ${kind}">${esc(text(value))}</span>`}
function header(title,subtitle){return `<header class="view-header"><div><span class="eyebrow">WORKSPACE SCOPED</span><h2>${esc(title)}</h2><p>${esc(subtitle)}</p></div><span class="read-lock">GET ONLY · NO WRITE</span></header>`}
function row(label,value){return `<div class="status-row"><span>${esc(label)}</span><b>${esc(text(value))}</b></div>`}
function setChrome(){const current=state.workspaces.find(item=>item.workspace_id===state.workspaceId);q("#page-title").textContent=current?`العمليات المحكومة — ${current.display_name}`:"العمليات المحكومة";q("#workspace-state").textContent=current?`${text(current.policy_pack_id)} · ${text(current.workspace_operations_binding)}`:"";q("#side-model").textContent="NONE";q("#side-pilot").textContent="NOT EXECUTED";document.querySelectorAll("[data-route]").forEach(link=>link.classList.toggle("active",link.dataset.route===state.route))}
function dashboard(data){const counts=data.counts||{};const metric=(label,value,note)=>`<article class="card metric"><small>${esc(label)}</small><strong>${esc(value??0)}</strong><p>${esc(note)}</p></article>`;return `${header("ملخص العمليات","كل الأرقام تخص مساحة العمل المختارة فقط، ولا ينتج هذا العرض أحداثًا أو انتقالات.")}<section class="metrics">${metric("مسودات",counts.draft,"سجل محفوظ محليًا")}${metric("صندوق الوارد",counts.inbox,"بانتظار قرار")}${metric("قيد المراجعة",counts.under_review,"مراجعة بشرية")}${metric("معتمدة",counts.approved,"لا تعني تنفيذًا")}</section><section class="layout-two"><article class="card panel"><h3>الحدود الفعالة</h3><div class="status-list">${Object.entries(data.controls||{}).filter(([key])=>["execution_gateway","model_execution","pilot_execution","tool_execution","cross_workspace_read","cross_workspace_write","human_review"].includes(key)).map(([key,value])=>row(key,value)).join("")||"<p>لا توجد ضوابط قابلة للعرض.</p>"}</div></article><article class="card panel"><h3>حالة الواجهة</h3><div class="status-list">${row("نوع الواجهة","READ FIRST")}${row("الإدخال عبر UI","LOCKED")}${row("التحكم بالهوية","FULL-STACK PENDING")}${row("مساحة العمل",state.workspaceId)}</div></article></section><article class="card lock-card"><span>🛡</span><div><h3>لماذا حُجبت نماذج الإنشاء؟</h3><p>لأن endpoints الكتابة الحالية في Governed Operations لا تحتوي بعد على Actor Principal وWorkspace Scope وClient Scope قسريين. الواجهة لا تتحول إلى قناة التفاف على الحوكمة.</p></div></article>`}
function tasks(items){const safeItems=Array.isArray(items)?items:[];return `${header("المهام المحكومة","قراءة المهام وتفاصيلها مع الحفاظ على العزل. الإنشاء والتحويلات غير متاحة في Frontend V1.")}<article class="card panel"><div class="toolbar"><div><h3>سجل المهام</h3><p>${esc(state.workspaceId)}</p></div><span class="pill info">${safeItems.length} عنصر</span></div>${safeItems.length?`<div class="table-wrap"><table><thead><tr><th>العنوان</th><th>الحالة</th><th>المخاطر</th><th>الإصدار</th><th></th></tr></thead><tbody>${safeItems.map(task=>`<tr><td><b>${esc(text(task.title,task.task_id))}</b><br><code>${esc(text(task.task_id))}</code></td><td>${pill(task.status)}</td><td>${pill(task.risk_level,["high","critical"].includes(task.risk_level)?"warn":"")}</td><td>${esc(text(task.version))}</td><td><button class="inline-button" data-task="${esc(text(task.task_id))}" type="button">عرض</button></td></tr>`).join("")}</tbody></table></div>`:`<div class="empty"><b>لا توجد مهام في هذه المساحة.</b><p>هذه النتيجة قراءة فقط وليست دعوة لإنشاء مهمة من دون سياق ممثل موثق.</p></div>`}</article><section id="detail" class="detail"></section>`}
function taskDetail(bundle){const task=bundle.task||{};return `<article class="card panel"><div class="toolbar"><div><span class="eyebrow">TASK BUNDLE</span><h3>${esc(text(task.title,task.task_id))}</h3></div>${pill(task.status)}</div><div class="status-list">${row("المعرف",task.task_id)}${row("المساحة",task.workspace_id)}${row("الحالة",task.status)}${row("الخطر",task.risk_level)}${row("بوابة الدليل",bundle.readiness?.evidence_gate)}${row("التنفيذ",task.execution_state)}</div><details><summary>سجل المراجعة والانتقالات</summary><pre>${esc(JSON.stringify({history:bundle.history,permission_intersection:task.permission_intersection},null,2))}</pre></details></article>`}
function reviews(items){const safeItems=Array.isArray(items)?items:[];return `${header("المراجعات البشرية","سجل مراجعة مقروء لمساحة العمل الحالية.")}<section class="stack">${safeItems.length?safeItems.map(item=>`<article class="card list-item"><div><b>${esc(text(item.task_id))}</b><p>${esc(text(item.rationale,"لا يوجد تبرير مرئي."))}</p></div><div>${pill(item.decision)}<small>${esc(text(item.reviewer_id))}</small></div></article>`).join(""):`<div class="empty">لا توجد مراجعات مسجلة.</div>`}</section>`}
function evidence(items){const safeItems=Array.isArray(items)?items:[];return `${header("الأدلة","الأدلة تنتمي للمساحة الحالية فقط. التسجيل من UI مقفل حتى اكتمال الحوكمة الخادمية.")}<section class="stack">${safeItems.length?safeItems.map(item=>`<article class="card list-item"><div><b>${esc(text(item.category))} · ${esc(text(item.display_status))}</b><p>${esc(text(item.summary))}</p><small>${esc(text(item.source_reference))}</small></div><div>${pill(item.trust_level)}<small>${esc(text(item.task_id,"GENERAL"))}</small></div></article>`).join(""):`<div class="empty">لا توجد أدلة قابلة للعرض في هذه المساحة.</div>`}</section>`}
function assurance(data){return `${header("سلامة التدقيق","Hash chain مستقل لكل مساحة عمل.")}<article class="card panel"><div class="status-list">${row("سلامة السلسلة",data.audit_chain_integrity)}${row("عدد الأحداث",data.audit_event_count)}${row("البصمة النهائية",data.terminal_hash||"—")}</div></article>`}
function controls(data){return `${header("الضوابط والسياسة","القراءة التقنية متاحة لمراجعة الحدود، وليس لتجاوزها.")}<article class="card panel"><pre>${esc(JSON.stringify(data,null,2))}</pre></article>`}
async function render(){try{setChrome();let html;if(state.route==="tasks"){html=tasks((await get(path("/tasks"))).items)}else if(state.route==="reviews"){html=reviews((await get(path("/reviews"))).items)}else if(state.route==="evidence"){html=evidence((await get(path("/evidence"))).items)}else if(state.route==="assurance"){html=assurance(await get(path("/integrity")))}else if(state.route==="controls"){html=controls(await get(path("/controls")))}else{html=dashboard(await get(path("/dashboard")))}q("#view").innerHTML=html;document.querySelectorAll("[data-task]").forEach(button=>button.addEventListener("click",async()=>{try{const bundle=await get(path(`/tasks/${encodeURIComponent(button.dataset.task)}`));q("#detail").innerHTML=taskDetail(bundle);q("#detail").scrollIntoView({behavior:"smooth",block:"start"})}catch(error){showError(error)}}))}catch(error){showError(error)}}
function showError(error){q("#view").innerHTML=`<article class="card panel"><span class="eyebrow">READ FAILURE</span><h2>تعذر تحميل البيانات المقروءة</h2><p>لم يتم إرسال طلب كتابة أو تشغيل أي قدرة. تحقق من الخادم المحلي ثم أعد المحاولة.</p><code>${esc(error.message)}</code></article>`}
async function bootstrap(){const registry=await get("/workspaces");state.workspaces=Array.isArray(registry.items)?registry.items:[];if(!state.workspaces.some(item=>item.workspace_id===state.workspaceId))state.workspaceId=state.workspaces[0]?.workspace_id||"palwakf_government";q("#workspace-select").innerHTML=state.workspaces.map(item=>`<option value="${esc(item.workspace_id)}">${esc(item.display_name)} — ${esc(item.classification)}</option>`).join("");q("#workspace-select").value=state.workspaceId;setChrome();await render()}
q("#workspace-select").addEventListener("change",event=>{state.workspaceId=event.target.value;history.replaceState(null,"",`/operations?workspace_id=${encodeURIComponent(state.workspaceId)}#${state.route}`);render()});
q("#refresh").addEventListener("click",render);
document.addEventListener("click",event=>{const link=event.target.closest("[data-route]");if(!link)return;event.preventDefault();state.route=link.dataset.route;history.replaceState(null,"",`/operations?workspace_id=${encodeURIComponent(state.workspaceId)}#${state.route}`);render()});
addEventListener("hashchange",()=>{state.route=location.hash.slice(1)||"dashboard";render()});
bootstrap().catch(showError);
})();