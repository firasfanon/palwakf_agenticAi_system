# Changelog — Command Center V1.2 Rev C

- Replaces the Rev B static gate with a text-source-only scanner.
- Excludes `__pycache__` and `.pyc` artifacts.
- Preserves all Command Center runtime files and FastAPI integration unchanged.
- Adds syntax and preflight gates plus exact preimage backup installer.
