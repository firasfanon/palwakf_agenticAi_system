# تقرير الاستكشاف والتصميم — شاشة البدء ولوحة العمليات

**المعرف:** `MEGA_BATCH_LOCAL_AGENTS_PRODUCT_START_SCREEN_AND_OPERATIONAL_CONSOLE_UX_UI_DISCOVERY_DESIGN_AND_GOVERNED_READ_ONLY_CANDIDATE_V1`  
**الحالة:** `CANDIDATE_STATIC_VALIDATION_PASS__NOT_APPLIED__NOT_RUNTIME_EXECUTED__NOT_UAT_EXECUTED`

## 1) قرار المنتج

تظل الواجهة تحت `/agent-console` واجهة **عرض محكوم فقط**. لكن العرض التقني السابق، الذي يطبع JSON داخل بطاقات، لا يشرح رحلة المستخدم ولا يميّز بصورة كافية بين الحالة، السياق، والأفعال المحجوبة.

المرشح يستبدل ذلك بمركز عمليات عربي أولًا يخدم ثلاثة أهداف:

1. **إظهار الحقيقة التشغيلية:** ما هو ظاهر، وما هو محجوب، ولماذا.
2. **تثبيت السياق:** مساحة العمل والفاعل والعميل حدود خادمية مستقبلية، وليست حقولًا يحفظها المتصفح.
3. **منع واجهة وهمية:** لا أزرار تنفيذ أو اعتماد أو إرسال تبدو عاملة وهي غير مخولة.

## 2) ما تغيّر في التصميم

| السطح | قبل المرشح | في المرشح |
|---|---|---|
| شاشة البداية | ملخص JSON تقني ومصطلحات مختلطة | Hero تشغيلي، مؤشرات حالة، مسار عمل محكوم، وحدود صريحة |
| التنقل | قائمة أساس طويلة تظهر كاملة على الهاتف | Sidebar مكتبي + Drawer للهاتف، مع تسميات عربية أولًا |
| الحالات | Loading/Denied/Error عامة | حالات مفهومة توضّح أن الرفض حد حوكمة لا عطل واجهة |
| المساعدون | عناصر JSON خام | بطاقات مختصرة: الاسم، نمط العمل، وحالة التنفيذ |
| مساحات العمل | قائمة تقنية مزدوجة | بطاقات سياق وسياسة وحالة lifecycle من دون اختيار أو تفعيل |
| المسارات المقيدة | رسالة Foundation عامة | صفحة توضيحية تبين ما هو مقفل وسبب القفل |

## 3) الهوية البصرية

- **الأساس:** أزرق سيادي عميق `#0B2E55` و`#071D37`.
- **التأكيد:** ذهبي وقفي `#E5BF70` و`#C79A3B`.
- **الحالات الحرجة:** أحمر ملكي `#B22222`، محصور في الحجب أو المخاطر، لا في عناصر التصفح العادية.
- **الخلفية:** محايدة فاتحة لتقليل الإجهاد البصري في لوحة تشغيل طويلة.
- **العربية:** RTL أصيل وتسميات عربية؛ المصطلحات التقنية لا تظهر إلا عند الحاجة وفي موضعها التشغيلي.

## 4) تدفق شاشة البداية

1. **Hero:** يصرح أن البيئة في وضع مراجعة آمنة، ويبين أن الهوية لا تُحمّل في المتصفح وأن التنفيذ والـPilot غير متاحين.
2. **مؤشرات:** مساحات العمل، مهام للمراجعة، الأدلة الظاهرة، وحالة تشغيل النموذج؛ كل قيمة تأتي من عقد قراءة أو تعرض حالة غير متاحة بصدق.
3. **مسار العمل:** اقرأ الحالة ← ثبّت السياق على الخادم ← راجع قبل القرار.
4. **المساعدون:** ملخص سجلات، لا إعدادات حساسة أو prompts أو مفاتيح.
5. **المساحات:** قراءة سياسات وحالات فقط، بلا dropdown لاختيار عميل أو مساحة.

## 5) الاستجابة للهاتف

التصميم يحل الخلل السابق: التنقل لم يعد شبكة كاملة تسبق المحتوى. عند العرض الضيق يظهر زر قائمة، ويصبح الـSidebar Drawer عند الطلب. المحتوى يبدأ بشريط العنوان ثم Hero ثم بطاقات مفردة.

## 6) العقود التقنية المحفوظة

- لم تتغير مسارات FastAPI أو mount تحت `/agent-console`.
- عميل HTTP ما زال يستعمل `GET` وحده و`credentials: "omit"`.
- لا `Authorization` أو `Bearer` أو تخزين `localStorage` أو `sessionStorage`.
- لا تغيير backend أو SQLite أو client_id أو commercial persistence.

## 7) معاينات بصرية

الصور ضمن `VISUAL_PREVIEWS/` هي **معاينات تصميم مشتقة من المصدر** باستخدام بيانات Mock محلية تعليمية. لا تمثل تشغيل FastAPI أو UAT Runtime ولا يجوز استخدامها كدليل قبول runtime.

## 8) قرار جاهزية

هذا المرشح صالح لمراجعة التصميم وتطبيق لاحق منفصل داخل isolated worktree. لا يصلح بعد كـbaseline مقبول ولا يفتح React write أو UAT تجاري أو تشغيل نموذج أو Pilot أو Production.
