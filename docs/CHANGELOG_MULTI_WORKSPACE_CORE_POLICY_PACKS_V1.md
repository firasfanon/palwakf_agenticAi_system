# Changelog — Multi-Workspace Core + Policy Packs V1

- Adds a standalone workspace core module and observability UI.
- Adds four versioned policy packs.
- Adds registry/audit contract initialized only on first workspace-core runtime access.
- Does not modify existing Command Center or Governed Operations behavior.
- Does not migrate legacy state.
