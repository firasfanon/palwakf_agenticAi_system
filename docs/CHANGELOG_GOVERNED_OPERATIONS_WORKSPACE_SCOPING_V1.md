# Changelog

- Added workspace-scoped operations API and UI.
- Added per-workspace task/evidence/audit SQLite isolation.
- Added policy + baseline-agent permission intersection snapshot.
- Retired unscoped operational write routes from the scoped contract; legacy data remains untouched.
- Added cross-workspace negative tests and payload injection rejection.
