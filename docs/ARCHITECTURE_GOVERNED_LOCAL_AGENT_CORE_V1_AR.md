# المعمارية

`Workspace → Policy Pack → Agent Registry → Permission Intersection → Deterministic Preparation → Review Packet → Audit Hash Chain`

الـCore ملك للنظام نفسه. لا يعتمد على Framework وكلاء خارجي ولا على مزود نموذج كسلطة. نموذج محلي قد يُربط في دفعة لاحقة فقط بعد اعتماد منفصل.
