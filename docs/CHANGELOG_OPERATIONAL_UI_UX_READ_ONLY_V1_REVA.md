# Changelog — Rev A

- Replaces Arabic runtime marker checks in PowerShell gates with ASCII-only markers.
- Adds `READ_ONLY_OPERATIONAL_DASHBOARD` marker to HTML and JavaScript.
- Preserves the same three-file static UI scope.
- No backend, API, router, store, task, model, or Pilot change.
