# Windows Local Browser UAT and Runtime Evidence V1

هذه إضافة تشغيلية للـbaseline المطبق. تضيف Runner وStatic Test ودليل أدلة؛ لا تعدّل backend أو React أو routes أو صلاحيات.

المسار الرسمي:

```text
scripts/Invoke-WindowsLocalBrowserUatAndRuntimeEvidenceV1.ps1
```

النتيجة المقبولة لا تساوي Production approval، ولا تفتح أي كتابة أو تشغيل مساعد.
