# تقرير فحص شاشة البدء والواجهة الحالية

## 1) النتيجة التنفيذية

واجهة React الحالية موجودة تحت `/agent-console`، وهي ليست شاشة منتج تشغيلية مكتملة بعد. إنها **Foundation read-only**: تعرض مركز قيادة تقنيًا، قائمة مساحات، تشخيصًا، وصفحات حوكمة مقفلة، من دون إدخال بيانات أو تنفيذ أو اعتماد أو تشغيل نموذج.

لا أوصي بالانتقال الآن إلى `Commercial Client Scope Apply`. الأولوية المنطقية هي اعتماد تصميم شاشة البدء ولوحة التشغيل أولًا، لأن الواجهة الحالية لا تشرح للمستخدم رحلة العمل ولا تميّز بوضوح بين الحالة، السياق، والأفعال المستقبلية المقيدة.

## 2) ما تعرضه شاشة البدء الحالية

### على سطح المكتب

- شريط جانبي RTL واسع باسم **المساعدون المحليون** مع علامة `LA` ووسم `React Foundation V1`.
- تسعة أقسام ثابتة: مركز القيادة، مساحات العمل، المهام، المشاريع، سجل الأدلة، المراجعات البشرية، الأدوات، التشخيص، والـPilot.
- بطاقة حوكمة ثابتة: `Default-Deny`، لا Token محفوظ، ولا تنفيذ نموذج.
- رأس صفحة باسم **مركز القيادة** مع شارات `React + TypeScript` و`Read-first`.
- Hero تقني يصرح بأن الواجهة تقرأ فقط، وأن الـPilot غير منفذ.
- بطاقتان تعرضان بيانات API مباشرة: ملخص مركز القيادة وسجل المساعدين.

### على الهاتف

عند عرض أقل من `620px` تتحول القائمة إلى عمود واحد كامل، ولا توجد قائمة قابلة للطي أو زر تنقل. لذلك يستهلك الشريط الجانبي معظم بداية الشاشة، ويظهر محتوى **مركز القيادة** بعد تمرير طويل. هذه ليست تجربة بداية مناسبة للهاتف.

## 3) ما هو مربوط فعليًا

| السطح | الحالة | مصدر القراءة |
|---|---|---|
| مركز القيادة | قراءة فقط | `/api/v1/local-agents/dashboard` و`/api/v1/local-agent-core/agents` |
| مساحات العمل | قراءة فقط | `/api/v1/workspaces` و`/api/v1/governed-operations/workspaces` |
| التشخيص | قراءة فقط | `/health` |
| المهام/المشاريع/الأدلة/المراجعات/الأدوات/الـPilot | صفحة ضبط وحوكمة فقط | لا طلب قراءة تشغيلي مرتبط حاليًا |

عميل React يستدعي `GET` فقط، مع `credentials: "omit"`، ولا يرسل Authorization/Bearer ولا يخزن Token أو حالة تشغيلية في المتصفح.

## 4) تقييم UX/UI

### عناصر مقبولة في المرحلة التأسيسية

- عزل واضح بين القراءة والتنفيذ.
- إظهار حالة الحوكمة وDefault-Deny للمشغل.
- RTL معلن في `index.html`.
- لا أزرار ظاهرية توحي بالتشغيل قبل اعتماد الصلاحيات.
- صفحات المسارات الأساسية كلها قابلة للتركيب الخادمي تحت `/agent-console`.

### نواقص تمنع اعتمادها كشاشة بداية منتجية

1. **الهوية البصرية:** اللون الأخضر الداكن وهوية `LA` لا ينسجمان مع هوية PalWakf المعتمدة: الأزرق/الذهبي مع الأحمر الملكي `#B22222` للحالات الحرجة أو المحجوبة.
2. **الرسالة الافتتاحية:** لا توجد شاشة ترحيب تحدد: أين أنا؟ ما هي مساحة عملي؟ ما الذي أستطيع فعله الآن؟ وما الذي يحتاج اعتمادًا؟
3. **البيانات الخام:** `DataCard` يعرض مفاتيح وJSON مباشرة، وهو مناسب للتشخيص وليس لمشغل أعمال.
4. **غياب سياق المستخدم:** لا يوجد اختيار أو عرض للـActor أو Workspace أو Client؛ وهذا صحيح أمنيًا الآن، لكنه يعني أن الواجهة ليست بعد لوحة تشغيل مستخدم.
5. **الهاتف:** القائمة العمودية الكاملة تسبق المحتوى؛ لا يوجد Drawer أو collapse أو bottom navigation.
6. **الصفحات المتوقعة:** صفحات المهام والمشاريع والأدلة والمراجعات والأدوات والـPilot تعرض Notice فقط، وليست أسطح عمل وظيفية.
7. **المصطلحات:** مزيج غير منضبط بين العربية والإنجليزية (`React Foundation V1`, `Read-first`, `Pilot`) لا يحقق لغة تشغيل عربية أولًا.

## 5) القرار المعماري المقترح

```text
COMMERCIAL_CLIENT_SCOPE_APPLY = DEFERRED_BY_PRODUCT_READINESS_DECISION
CURRENT_REACT_FOUNDATION = PRESERVE_AS_READ_ONLY_REFERENCE
NEXT_FOCUS = START_SCREEN_AND_OPERATIONAL_CONSOLE_UX_UI_DISCOVERY_DESIGN
NO_REACT_WRITE = CONTINUES
NO_MODEL_EXECUTION = CONTINUES
NO_PILOT = CONTINUES
```

## 6) الدفعة التالية الموصى بها

```text
MEGA_BATCH_LOCAL_AGENTS_PRODUCT_START_SCREEN_AND_OPERATIONAL_CONSOLE_UX_UI_DISCOVERY_DESIGN_AND_GOVERNED_READ_ONLY_CANDIDATE_V1
```

### نطاقها المقترح

- تصميم شاشة بدء عربية أولًا ذات هوية PalWakf.
- فصل واضح بين: الملخص التشغيلي، مساحة العمل، العملاء، طوابير التحضير، المراجعة، الأدلة، وحالة الحوكمة.
- تحويل JSON الخام إلى بطاقات حالة وقوائم قابلة للقراءة.
- تصميم هاتف حقيقي: Drawer أو Bottom Navigation، مع إبقاء الحوكمة ظاهرة دون أن تحجب المحتوى.
- تحديد حالات Empty/Denied/Loading/Error بصريًا.
- Mock read-only محكوم للعرض فقط؛ بلا إدخال بيانات أو كتابة أو Token أو تنفيذ.
- إعداد Candidate مستقل مع Static Gate وVisual UAT Plan، من دون Apply إلا بتفويض لاحق.

## 7) ما لا يدخل في الدفعة التالية

```text
NO_COMMERCIAL_CLIENT_SCOPE_APPLY
NO_SQLITE_MIGRATION
NO_REACT_WRITE_UI
NO_AUTH_TRANSPORT
NO_MODEL_EXECUTION
NO_PILOT
NO_PRODUCTION
```
