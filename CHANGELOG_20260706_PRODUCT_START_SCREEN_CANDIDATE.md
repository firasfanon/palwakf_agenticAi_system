# Changelog — 2026-07-06

## أضيف

- Product Read-Only Candidate لشاشة البدء ولوحة العمليات تحت `/agent-console`.
- تصميم عربي RTL بهوية أزرق/ذهبي، وأحمر ملكي للحالات المحجوبة.
- Responsive sidebar/drawer للهاتف.
- مكونات React قابلة لإعادة الاستخدام: `Icon`، `OperationalPanels`، وطبقة `presentation` لعزل بيانات API الخام عن العرض.
- صفحات توضيحية للمسارات المقيدة بدلاً من لوحات JSON التقنية.
- معاينات بصرية مشتقة من التصميم، وخطة UAT لنظام Windows لاحقًا.

## لم يتغير

- FastAPI/backend/mount routes.
- SQLite وقواعد client_id التجارية.
- العقود التشغيلية: `GET` فقط، `credentials: omit`، ولا Token في المتصفح.
- React write، Model، Pilot، Commercial Apply، Production.
